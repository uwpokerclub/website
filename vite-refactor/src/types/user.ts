/**
 * User is the JSON object returned from the API.
 */
export type User = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  faculty: string;
  questId: string;
  createdAt: string;
};

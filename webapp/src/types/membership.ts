export type Membership = {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  userId: string;
  semesterId: string;
  paid: boolean;
  discounted: boolean;
  attendance: number;
};
